# X launch thread (spec §51)

Rights-clean assets throughout: the original `seed-tinkers` film (hero
cut + full launch film, no licensed music), with the IP-styled
experiments referenced only as "what people have made" if at all.

## Post 1 — the hook (attach: 13s square hero cut, loops, muted)

> I made reality branch. live.
>
> one moment → 2 → 4 → 8 parallel timelines, generated faster than you
> can watch them.
>
> every scene contains infinite worlds. open source ↓

## Reply 1 — the proof (attach: 47s launch film with the status panel)

> each next scene is storyboarded by an LLM and rendered in ~4 seconds —
> before the current one finishes playing. the log on the right is the
> generation happening live.
>
> click any universe to steer the story into it, with sound.

## Reply 2 — the agent loop

> give the repo + your video to your coding agent:
>
> "clone github.com/lizzij/multiverse-engine, read RUN_WITH_AGENT.md,
> and run me a live multiverse from my clip."
>
> it will generate the multiverse for you.

## Reply 3 — participation (attach: participate export)

> I split one moment into 8 realities.
>
> which one should continue? reply 1–8.
>
> (winner gets branched tomorrow — `multiverse branch RUN --node N`)

## Follow-up post (next day)

> you chose N.
>
> reality N just split. going deeper.

Notes:
- post the hero square first; X favors short loops with high completion.
- the live player/stream link goes in the bio + reply 1, not post 1
  (external links in the root post depress reach).
- have the winner's branch pre-generated before announcing the vote
  (spec §50): `multiverse branch <run> --node <n>` for 2-3 likely
  winners costs a few cycles and removes the latency between "you
  chose" and the follow-up.
